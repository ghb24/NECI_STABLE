tau-values
    (
        'start', {x, 'from_popsfile', ('tau_factor', x), 'diagonal_deterministic'},
        ['min', x],
        ['max', x]
    )

tau-search
    {
        'off'
        'algorithm'
            'conventional'
            'histogramming',
                [(1 - x), x, x],
        'stop'
            (['if_no_change', n], ['if_var_shift'], ['after_iter', n]),
    }
