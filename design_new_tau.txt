tau-values
    (
        ['start', {x, 'from_popsfile', ('tau_factor', x), 'diagonal_deterministic'}],
        ['min', x],
        ['max', x]
    )

tau-search
    {
        'conventional'
        'histogramming',
            [(1 - x), x, x],
        'off'

        (['stop_if_no_change', n], ['stop_if_var_shift'], ['stop_after_iter', n]),
    }
